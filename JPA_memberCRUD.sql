create database JPA_Database;
use  JPA_Database;
create table Member(
Id int primary key  auto_increment  not null,
name varchar(45),
username varchar(45),
password  varchar(45),
phone varchar(45),
address varchar(255));






